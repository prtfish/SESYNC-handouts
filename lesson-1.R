## Configure git

system("git config --global user.name 'Diana'")
system("git config --global user.email 'diana.weber@gmail.com'")

## Set a new origin URL

system('git remote set-url origin https://github.com/prtfish/SESYNC-handouts.git')